{
  "name": "homepage",
  "version": "1.0.0",
  "description": "Patatoa's personal start page.",
  "author": "Patrick",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "npm install; nuxt build; nuxt start",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "express": "^4.17.1",
    "foundation-sites": "^6.6.3",
    "nuxt": "^2.12.2"
  },
  "devDependencies": {
    "cross-env": "^5.2.1",
    "nodemon": "^1.19.4"
  }
}
